# Sample data

No static price dump is committed. `python scripts/seed.py` generates the fixed synthetic dataset directly into the configured database, making its provenance and idempotency testable.
