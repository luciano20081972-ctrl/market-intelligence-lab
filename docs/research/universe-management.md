# Universe management

Research universes are workspace/system-owned definitions with selection rules and source provenance. Immutable versions have effective and simulation-eligible periods plus membership checksums. Each membership has its own half-open validity interval, eligibility time, manifest reference, and provenance. Historical research never substitutes current constituents for the version visible at T. Deterministic fixtures avoid live index-membership licensing.
