# Research Regimes

Regime definitions are versioned, declarative rules. Assignments record their observation and simulation-eligible times, so a future classification cannot leak into a historical study. Regimes describe context; they do not guarantee that a remembered relationship will persist.
