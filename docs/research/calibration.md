# Forecast calibration

Calibration is empirical, cohort-specific, versioned, reproducible, and as-of bounded. Every output exposes evaluation mode, sample count, time range, bins, uncertainty, and methodology. Status progresses from `INSUFFICIENT_SAMPLE` through `PRELIMINARY`, `ESTABLISHING`, and `USABLE`; stale cohorts are labeled rather than silently reused.
