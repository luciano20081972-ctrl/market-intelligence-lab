# Feature lineage

Each feature value links to a grouped input manifest, source observation references, source-manifest IDs, graph-relationship IDs, evidence IDs, computation version, and canonical checksum. Manifest-level grouping avoids an explosive row per raw numeric input while retaining reproducibility. The lineage API and viewer expose this chain without arbitrary SQL.
