# RD-Agent research engine

The v0.10 RD-Agent prototype is a disabled, artifact-only boundary. A future isolated runner may receive a bounded research brief and return captured artifacts; it cannot query MIL databases, execute in the API process, access production secrets, use an unrestricted filesystem, or auto-merge code. Returned candidates start—not bypass—the standard validation pipeline.
