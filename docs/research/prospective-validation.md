# Prospective validation

A `PROSPECTIVE` forecast is recorded and locked before `outcome_eligible_time`. `HISTORICAL_REPLAY` and `FIXTURE` are separate immutable populations and never improve prospective statistics. Outcome attachment before maturity is rejected. Revisions create new observations; corrections create superseding forecasts. Historical validation is not prospective evidence.
