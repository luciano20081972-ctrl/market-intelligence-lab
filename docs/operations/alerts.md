# Operational alerts

Alerts use INFO, WARNING, ERROR, or CRITICAL and deduplicate by workspace, condition key, and open
status. They explain impact, unaffected capability, and a recommended action. Conditions include
offline workers/scheduler, stale critical datasets, provider authentication failures, repeated
ingestion failures, disk pressure, migration mismatch, backup/restore overdue, and queue backlog.
