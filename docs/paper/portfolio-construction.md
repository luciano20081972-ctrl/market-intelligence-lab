# Paper portfolio construction

Bounded methods include equal weight, score capped, volatility scaled, risk parity, and minimum variance. The existing skfolio adapter may propose weights, but hard risk is authoritative. Allocation priority exposes components and formula version and is not expected alpha or profit probability. Plans freeze benchmark, candidate set, weights, policy, assumptions, preview, and checksum.
